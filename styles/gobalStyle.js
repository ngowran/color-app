import { StyleSheet } from 'react-native';

module.exports = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    heading: {
        fontSize: 30,
        color: '#000',
        fontWeight: 'bold',
        marginBottom: 20,
    },
    text: {
        fontSize: 20,
        color: '#000',
    },
});